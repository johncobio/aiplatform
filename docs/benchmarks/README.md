# Benchmarks

Every file here records measurements that were actually taken, with the
command used, the hardware, and the date. Nothing here is estimated unless the
word "estimate" appears next to the number.
